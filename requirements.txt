# API
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4

# ML
scikit-learn==1.5.0
xgboost==2.0.3
pandas==2.2.2
numpy==1.26.4
joblib==1.4.2

# Persistence & cache (optional at runtime — the service degrades gracefully
# when FDP_DATABASE_URL / FDP_REDIS_URL are unset, e.g. in CI and local tests).
SQLAlchemy==2.0.31
psycopg[binary]==3.2.1
redis==5.0.7

# AWS SDK — used to emit CloudWatch custom metrics (no-op without credentials).
boto3==1.34.144

# Testing
pytest==8.2.2
httpx==0.27.0
